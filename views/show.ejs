

    <p>Name: <%= item.name %></p>
    <p>Description: <%= item.description %></p>
    <p>Img: <%= item.img %></p>
    <p>Price: <%= item.price %></p>
    <p>Quantity: <%= item.qty %></p>
    <% if(item.qty > 0) { %>
        <form action='/chunks/<%= item.id %>/buy?_method=PUT' method="POST">
            <input type="submit" value="BUY ME" />
        </form>
    <% }else { %>
        <form action='/chunks/<%= item.id %>/buy?_method=PUT' method="POST">
            <input disabled type="submit" value="BUY ME" />
        </form>
    <% } %>

    <a href="/chunks">HOME</a>
